{
  "name": "largs-org",
  "version": "0.1.0",
  "private": true,
  "description": "largs.org — community reference site for Largs, North Ayrshire",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "fetch": "node scripts/fetch-data.mjs",
    "build": "eleventy",
    "serve": "eleventy --serve",
    "start": "npm run fetch && npm run serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
